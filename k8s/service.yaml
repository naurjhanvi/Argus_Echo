apiVersion: v1
kind: Service
metadata:
  name: argus-echo
  namespace: argus-echo
spec:
  type: LoadBalancer
  selector:
    app: argus-echo
  ports:
    - name: http
      port: 80
      targetPort: 8501
